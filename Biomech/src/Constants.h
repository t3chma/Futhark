#pragma once

enum class Type {
	PROP = 0,
	PLAYER = 1,
	SWARM,
	PRACTICE,
	BOSS
};

enum class Team {
	NONE = 0,
	PLAYER = 1,
	PEACE,
	BANDIT,
	PREY,
	PRED
};

enum class Spec {
	PROP = 0,
	PLAYER = 1,
	MOUSE,
	BOT,
	DUMMY
};